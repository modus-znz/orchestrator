{
  "name": "@orchestrator/daemon",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "orchestratord": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.test.json",
    "test": "vitest run",
    "dev": "node --experimental-strip-types src/index.ts"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
